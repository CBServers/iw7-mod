name: Build

on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "*"
    types: [opened, synchronize, reopened]
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: false
jobs:
  build:
    name: Build binaries
    runs-on: windows-2025
    strategy:
      matrix:
        configuration:
        #  - Debug
          - Release
    steps:
      - name: Set Git to use LF line endings
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf

      - name: Check out files
        uses: actions/checkout@v6
        with:
          submodules: true
          fetch-depth: 0
          # NOTE - If LFS ever starts getting used during builds, switch this to true!
          lfs: true

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v2.0.0

      - name: Generate project files
        run: tools/premake5 vs2022

      - name: Set up problem matching
        uses: ammaraskar/msvc-problem-matcher@master

      - name: Build ${{matrix.configuration}} binaries
        run: msbuild /m /v:minimal /p:Configuration=${{matrix.configuration}} /p:Platform=x64 build/iw7-mod.sln

      - name: Upload ${{matrix.configuration}} Binary
        uses: actions/upload-artifact@v4
        with:
          name: ${{matrix.configuration}} Binary
          path: |
            build/bin/x64/${{matrix.configuration}}/iw7-mod.exe
          #  build/bin/x64/${{matrix.configuration}}/iw7-mod.pdb
            
      - name: Upload ${{matrix.configuration}} data artifacts
        uses: actions/upload-artifact@v6
        with:
          name: ${{matrix.configuration}} data artifacts
          path: |
            data/cdata/*

  deploy:
    name: Deploy artifacts
    needs: build
    runs-on: windows-2022
    if: github.repository_owner == 'CBServers' && github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    steps:
      - name: Set Git to use LF line endings
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
          
      - name: Download Release binaries
        uses: actions/download-artifact@v4.1.8
        with:
          name: Release Binary

      - name: Download Release data artifacts
        uses: actions/download-artifact@v4.1.8
        with:
          name: Release data artifacts
          path: cdata

      - name: Download hashing executable
        run: |
          Invoke-WebRequest -Uri "https://github.com/CBServers/updater/releases/latest/download/update-hasher.exe" -OutFile "update-hasher.exe"

      - name: Generate hashed.json
        run: |
          .\update-hasher.exe

      - name: Set channel names
        run: |
          if ("${{ github.ref }}" -eq "refs/heads/develop") {
            echo "DATA_FOLDER=data-dev" >> $env:GITHUB_ENV
            echo "FILES_NAME=files-dev.json" >> $env:GITHUB_ENV
          } else {
            echo "DATA_FOLDER=data" >> $env:GITHUB_ENV
            echo "FILES_NAME=files.json" >> $env:GITHUB_ENV
          }

      - name: Create folder structure and rename files
        run: |
          mkdir -p iw7-mod/${{ env.DATA_FOLDER }}

          Copy-Item -Path .\cdata -Destination .\iw7-mod\${{ env.DATA_FOLDER }}\ -Recurse -Force
          
          Copy-Item -Path .\iw7-mod.exe -Destination .\iw7-mod\${{ env.DATA_FOLDER }}\
          
          Copy-Item -Path .\hashed.json -Destination .\iw7-mod\${{ env.FILES_NAME }}

      - name: Clone updater
        run: |
          git config --global user.name 'CBServers'
          git config --global user.email '${{ secrets.OWNER_EMAIL }}'
          $env:GIT_TERMINAL_PROMPT=0
          $env:GIT_USERNAME="CBServers"
          $env:GIT_PASSWORD="${{ secrets.REPO_PAT }}"
          git clone https://$env:GIT_USERNAME:$env:GIT_PASSWORD@github.com/CBServers/updater.git

      - name: Copy files to updater and cleanup
        run: |
          if (-Not (Test-Path -Path ".\updater\updater\iw7-mod")) {
            New-Item -Path ".\updater\updater\iw7-mod" -ItemType Directory -Force
          }
            
          # Wipe this channel so files dropped from the build stop being shipped
          $channel = ".\updater\updater\iw7-mod\${{ env.DATA_FOLDER }}"
          if (Test-Path -Path $channel) {
            Remove-Item -Path $channel -Recurse -Force
          }

          Copy-Item -Path .\iw7-mod\* -Destination .\updater\updater\iw7-mod\ -Recurse -Force
          
          Remove-Item -Path .\iw7-mod -Recurse -Force -ErrorAction SilentlyContinue
          Remove-Item -Path .\update-hasher.exe -Force -ErrorAction SilentlyContinue
          Remove-Item -Path .\hashed.json -Force -ErrorAction SilentlyContinue
          Remove-Item -Path .\cdata -Recurse -Force -ErrorAction SilentlyContinue

      - name: Push changes to updater
        run: |
          cd updater
          git add .
          git commit -m "Update artifacts"
          git push origin main